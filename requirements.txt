# Install torch CPU-only to avoid large CUDA downloads:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# Or with CUDA:
#   pip install torch
torch>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
pyyaml>=6.0
